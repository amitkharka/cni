import Header from './Component-Header.js';
import Container from './Component-Container.js';

const root = document.getElementById('root');
root.appendChild(Header.latest);
root.appendChild(Container.latest);